/** Each banner position rotates through exactly this many creatives. */
export const BANNER_SLIDES_PER_POSITION = 3;

/** Fixed interval so every slide gets equal screen time (round-robin). */
export const BANNER_SLIDE_INTERVAL_MS = 6000;
